automation SS_notifier:
  - alias: SS_notifier
  trigger:
    - platform: mqtt
      topic: socket_settings/notifier
  action:
    - service: persistent_notification.create
    d ata_template:
        title: "{{ trigger.payload.title }}"
        message: "{{ trigger.payload.msg }}"